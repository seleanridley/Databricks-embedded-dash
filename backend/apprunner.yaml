version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  runtime-version: 3.8
  command: gunicorn -b 0.0.0.0:$PORT app:app
  network:
    port: 5000
  env:
    - name: PORT
      value: "5000"
